version: "3"
services:
  backend:
    image: backend
    networks:
      - allnetwork

  database:
    image: mysql:8.0.23
    networks:
      - allnetwork
    environment:
      - MYSQL_ROOT_PASSWORD=Newnismo_2001
      - MYSQL_DATABASE=carsdb
      - MYSQL_USER=root
    volumes:
      - ~/backend_data:/public/product-img

networks:
  default:
    external:
      name: allnetwork
